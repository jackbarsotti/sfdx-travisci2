import { EventEmitter } from 'events';
import { Readable } from 'stream';
export declare const SALESFORCE_CERT_FINGERPRINT: string;
export declare function validSalesforceHostname(url: string | null): boolean;
export declare function validateRequestCert(request: EventEmitter): void;
export declare class CodeSignInfo {
    private _dataToSignStream;
    private _privateKeyStream;
    dataToSignStream: Readable;
    privateKeyStream: Readable;
}
export declare class CodeVerifierInfo {
    private _signatureStream;
    private _publicKeyStream;
    dataToVerify: Readable;
    private _dataToVerify;
    signatureStream: Readable;
    publicKeyStream: Readable;
}
export default function sign(codeSignInfo: CodeSignInfo): Promise<string>;
export declare function verify(codeVerifierInfo: CodeVerifierInfo): Promise<boolean>;
