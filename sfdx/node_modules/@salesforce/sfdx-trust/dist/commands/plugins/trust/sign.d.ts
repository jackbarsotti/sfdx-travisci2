import { FlagsConfig, SfdxCommand } from '@salesforce/command';
import { SigningResponse } from '../../../codeSigning/packAndSign';
/**
 * Help message for the command.
 * Help doesn't currently work for builtin commands this is here in case it ever does.
 */
export declare class Sign extends SfdxCommand {
    static description: string;
    static examples: string[];
    static hidden: true;
    protected static flagsConfig: FlagsConfig;
    protected static requiresUsername: boolean;
    protected static supportsDevhubUsername: boolean;
    protected static requiresProject: boolean;
    run(): Promise<SigningResponse>;
}
