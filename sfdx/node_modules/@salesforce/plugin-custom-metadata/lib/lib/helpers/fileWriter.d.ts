export declare class FileWriter {
    /**
     * Using the given file system, creates a file representing a new custom metadata type.
     * @param fs
     * @param devname
     * @param objectXML
     */
    writeTypeFile(corefs: any, dir: string, devName: string, objectXML: string): Promise<{
        dir: string;
        fileName: string;
        updated: boolean;
    }>;
    /**
     * Using the given file system, creates a file representing a new field for the given custom metadata type
     * @param fs
     * @param fieldname
     * @param fieldXML
     */
    writeFieldFile(corefs: any, dir: any, fieldName: any, fieldXML: any): Promise<{
        dir: string;
        fileName: string;
        updated: boolean;
    }>;
    createDir(dir: any): any;
}
