export declare class FileWriter {
    private writeQueue;
    private appendQueue;
    private tmpFilelist;
    private newFiles;
    constructor();
    queueWrite(filepath: string, content: string, options?: object): void;
    queueAppend(filepath: string, toAppend: string, options?: object): void;
    writeFiles(): void;
    private revertChanges;
    private removeTempFiles;
    private getHash;
}
